source venv/bin/activate
python -m app.ai_workers.workflow.full_workflow
celery -A celery_worker.celery worker --loglevel=info
curl -X POST http://localhost:5001/test-workflow   -H "Content-Type: application/json"   -d '{
    "user_id": "test_user_001",
    "prompt": "Organize a local 3-day Python coding bootcamp."
  }'
cd grpc_servers/user_handle_agent
cd app/proto 
python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. agent.proto

python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. agent.proto
# IMPORTANT: always use the venv python so sentence_transformers and other deps are available
source venv/bin/activate && python run.py
cloudflared tunnel --url http://localhost:8080
npx webpack
npx http-server ./app/src -p 8080

